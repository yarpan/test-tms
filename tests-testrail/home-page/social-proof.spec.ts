// import { test, expect } from '@playwright/test';

// test.describe('Social Proof', () => {
//   test.beforeEach(async ({ page }) => {
//     await page.goto('https://testomat.io/');
//   });

//   test('C88 shows the trusted-by section', async ({ page }) => {
//     await expect(page.getByText(/Trusted by/i).first()).toBeVisible();
//   });

//   test('C89 mentions tests run daily', async ({ page }) => {
//     await expect(page.getByText(/tests run daily/i).first()).toBeVisible();
//   });

//   test('C90 shows the recent reviews section', async ({ page }) => {
//     await expect(page.getByText(/reviews/i).first()).toBeVisible();
//   });
// });
