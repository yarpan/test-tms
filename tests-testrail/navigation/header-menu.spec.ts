// import { test, expect } from '@playwright/test';

// test.describe('Header Menu', () => {
//   test.beforeEach(async ({ page }) => {
//     await page.goto('https://testomat.io/');
//   });

//   test('C97 shows the Pricing link', async ({ page }) => {
//     await expect(page.getByRole('link', { name: 'Pricing' }).first()).toBeVisible();
//   });

//   test('C98 shows the Docs link', async ({ page }) => {
//     await expect(page.getByRole('link', { name: 'Docs' }).first()).toBeVisible();
//   });

//   test('C99 shows the Blog link', async ({ page }) => {
//     await expect(page.getByRole('link', { name: 'Blog' }).first()).toBeVisible();
//   });
// });
