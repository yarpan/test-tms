// import { test, expect } from '@playwright/test';

// test.describe('Footer Info', () => {
//   test.beforeEach(async ({ page }) => {
//     await page.goto('https://testomat.io/');
//   });

//   test('shows the company location', async ({ page }) => {
//     await expect(page.getByText(/Krak/i).first()).toBeVisible();
//   });

//   test('shows the contact email', async ({ page }) => {
//     await expect(
//       page.getByRole('link', { name: 'contact@testomat.io' }).filter({ visible: true }).first()
//     ).toBeVisible();
//   });

//   test('shows the copyright notice', async ({ page }) => {
//     await expect(page.getByText(/All rights reserved/i).first()).toBeVisible();
//   });
// });
